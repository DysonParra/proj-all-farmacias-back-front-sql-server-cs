@model IEnumerable<Project.Models.Barrio>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StrNombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IntIdCiudad)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StrNombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IntIdCiudad)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IntIdBarrio">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IntIdBarrio">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IntIdBarrio">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
