@model IEnumerable<Project.Models.Ciudad>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IntIdDane)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IntIdEstado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StrEstado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StrNombre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IntIdDane)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IntIdEstado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StrEstado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StrNombre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IntIdCiudad">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IntIdCiudad">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IntIdCiudad">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
